services:
  - type: web
    name: crimsonTrace-backend
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app.main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: NGROK_AUTH_TOKEN
        sync: false
